# 本地启动（Windows）

在 PowerShell 中执行：

```powershell
cd C:\Users\zxq\Documents\Codex\2026-07-17\ni-h\apps\web

# 若 npm.ps1 被执行策略拦截，使用 npm.cmd
npm.cmd install
npm.cmd run db:generate
npm.cmd run db:push
npm.cmd run db:seed
npm.cmd run dev
```

浏览器打开：http://localhost:3000

演示账号：
- 邮箱：demo@growmate.app
- 密码：demo123456

可选：在 `apps/web/.env` 配置 `OPENAI_API_KEY` 启用真实大模型陪伴对话。
